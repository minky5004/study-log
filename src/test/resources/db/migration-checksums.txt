# 마이그레이션 파일의 Flyway checksum. 이 목록이 기댓값의 정본이다.
#
# 형식은 `파일명 checksum` 한 줄씩이고 `#` 로 시작하는 줄과 빈 줄은 건너뛴다.
#
# 값을 고치는 것이 정당한 경우는 하나뿐이다 — 아직 어디에도 배포되지 않은 마이그레이션.
# 이미 배포된 파일이면 목록이 아니라 파일을 되돌린다. 근거는
# src/main/resources/db/migration/NOTES.md 에 있다.
#
# V1 의 값은 운영 Neon 의 flyway_schema_history 가 실제로 가진 값이다(2026-08-16 실측).
# 나머지 줄은 작업 트리에서 계산한 값이다.

V1__baseline_schema.sql 1981831069
V2__study_plan.sql -1361260748
V3__persistent_logins.sql 398004051
